//Escreva um operador ternário que compare duas strings e retorne a mensagem "Iguais" se forem iguais ou "Diferentes" caso contrário.

const stringOne = 'Juliana';
const stringTwo = 'Mario';

const isEqual = (stringOne === stringOne) ? 'iguais' : 'diferentes'; 

console.log(isEqual); 
